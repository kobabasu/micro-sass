/**
 * node_modulesが存在するディレクトリからの
 * ドキュメントルートを指定
 *
 * nameはtask名の先頭になる
 */

const root = './admin/';

export const dir = {
  name: '',

  root: root,
  src:  root + 'sass/',
  dist: root + 'css/',
  docs: root + 'docs/sass/'
}
